Scriptname SLH_ANIM_FemaleSensual extends sslAnimationFactory  

function LoadAnimations()
	; Prepare factory resources (as non creature)
;	PrepareFactory()
	; Missionary
;	RegisterAnimation("FemaleSensual")
endFunction



function FemaleSensualAnim(int id)
;	sslBaseAnimation Base = Create(id)

;	Base.Name = "Female sensual"

;	Base.SetContent(Sexual)
;	Base.SoundFX = Squishing

;	int a1 = Base.AddPosition(Female, addCum=VaginalAnal)
;	Base.AddPositionStage(a1, "ZaZHornyA", 0)
;	Base.AddPositionStage(a1, "ZaZHornyB", 0)
;	Base.AddPositionStage(a1, "ZaZHornyC", 0)
;	Base.AddPositionStage(a1, "ZaZHornyD", 0)
;	Base.AddPositionStage(a1, "ZaZHornyE", 0)

;	Base.AddTag("Masturbation")
;	Base.AddTag("Female")
;	Base.AddTag("Sensual")
;	Base.AddTag("PCKnownAnim")

;	Base.Save(id)


endFunction